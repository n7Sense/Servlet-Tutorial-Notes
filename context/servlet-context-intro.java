	ServletContext
=======================

	1. ServletContext context is global for entire application, where as ServletConfig is the local par servlet that means
	each servlet having one ServletConfig object,
	2. one servlet having one ServletConfig object, where as ServletContext is entire application created only one
		object created
	3. 	

	